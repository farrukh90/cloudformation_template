AWSTemplateFormatVersion: "Version Date"
  "Identifies the capability of the template"
  "The latest template version is 2010-09-09"
  "The value for the template format version declaration must be literal"
  JSON: "AWSTemplateFormatVersion":"2010-09-09"
  YAML: AWSTemplateFormatVersion: "2010-09-09"

Description:
  String
  This is optional it can be whatever I want
  Example: "This is dev environment"

Metadata:
  template Metadata

Mappings:
  set of parameters
Conditions:
  set of Conditions

Transform:
  set of Transforms

Recources:
  set of Recources

Outputs:
  set of Outputs
